# Core LangChain packages
# Versions are pinned via: constraints-langchain.txt
langchain>=0.2.0,<0.3.0
langchain-core>=0.2.0,<0.3.0
langchain-community>=0.2.0,<0.3.0
langchain-openai>=0.1.0,<0.2.0

# Database
neo4j>=5.0.0,<6.0.0

# Neo4j vector store (optional - for add_embeddings.py hybrid retrieval).
# Omitted from main deps: langchain-neo4j>=0.6 requires langchain-core>=1.0, which conflicts
# with our pinned langchain 0.2.x stack. Install separately if needed:
#   pip install langchain-neo4j  # may require upgrading langchain packages
#
# CentOS 7 compatibility
# langchain -> SQLAlchemy -> greenlet
# Newer greenlet versions may not provide a compatible wheel on older distros,
# causing pip to fall back to building from source (and fail with old toolchains).
greenlet==2.0.2

# Telegram Bot
python-telegram-bot>=20.0,<21.0

# Environment management
python-dotenv>=1.0.0,<2.0.0

# PDF text extraction (for add_doc_to_source script)
pypdf>=4.0.0,<6.0.0

# Retry logic
tenacity>=8.0.0,<9.0.0

# Structured logging
structlog>=23.0.0,<24.0.0

# Testing (development)
pytest>=7.0.0,<8.0.0
pytest-asyncio>=0.21.0,<1.0.0
pytest-mock>=3.10.0,<4.0.0

# Code quality (development)
black>=23.0.0,<24.0.0
ruff>=0.1.0,<1.0.0
mypy>=1.5.0,<2.0.0

# Monitoring (optional)
prometheus-client>=0.18.0,<1.0.0
